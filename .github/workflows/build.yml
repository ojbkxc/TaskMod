name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r27c

      - name: Install cargo-ndk
        run: cargo install cargo-ndk

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            server/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('server/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Update version
        id: version
        run: |
          # 从 tag 提取版本号 (v4.0 -> 4.0)
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

          # 计算 versionCode (4.0 -> 400, 1.2.3 -> 102003)
          IFS='.' read -ra PARTS <<< "$VERSION"
          VERSION_CODE=0
          for i in "${!PARTS[@]}"; do
            MULT=$((10 ** (6 - i * 3)))
            VERSION_CODE=$((VERSION_CODE + PARTS[i] * MULT))
          done
          echo "versionCode=$VERSION_CODE" >> $GITHUB_OUTPUT

          # 更新 module.prop
          sed -i "s/^version=.*/version=$VERSION/" module.prop
          sed -i "s/^versionCode=.*/versionCode=$VERSION_CODE/" module.prop

          # 更新 Cargo.toml
          sed -i "0,/^version = \".*\"/s/^version = \".*\"/version = \"$VERSION\"/" server/Cargo.toml

          echo "Updated version to $VERSION (code: $VERSION_CODE)"

      - name: Build
        working-directory: server
        run: cargo ndk -t arm64-v8a build --release

      - name: Package
        id: package
        run: |
          VERSION=${{ steps.version.outputs.version }}

          # 将编译产物复制到模块目录
          mkdir -p bin/arm64
          cp server/target/aarch64-linux-android/release/taskmod-server bin/arm64/
          chmod +x bin/arm64/taskmod-server

          # 打包 Magisk 模块 (从仓库根目录打包)
          zip -r TaskMod-${VERSION}.zip . \
            -x ".git/*" \
            -x ".github/*" \
            -x "server/*" \
            -x ".gitignore" \
            -x "bin/.gitkeep"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: TaskMod-${{ steps.version.outputs.version }}
          path: TaskMod-${{ steps.version.outputs.version }}.zip

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: TaskMod ${{ steps.version.outputs.version }}
          body: |
            ## TaskMod ${{ steps.version.outputs.version }}

            ### 功能
            - 定时任务调度
            - Web 管理面板 (端口 8080)
            - 截图管理 (系统 screencap)
            - 邮件通知

            ### 安装
            1. 下载 `TaskMod-${{ steps.version.outputs.version }}.zip`
            2. 通过 Magisk 刷入
            3. 浏览器访问 `http://设备IP:8080`
          files: TaskMod-${{ steps.version.outputs.version }}.zip
          draft: false
          prerelease: false
